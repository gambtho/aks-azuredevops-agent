apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vstsagent.fullname" . }}-test-connection"
  labels:
{{ include "vstsagent.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vstsagent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
